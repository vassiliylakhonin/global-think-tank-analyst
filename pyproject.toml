[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "global-think-tank-analyst"
version = "1.4.0"
description = "Enterprise-grade AI toolkit for strategic-risk analysis featuring LangGraph MoA, MCP server, and autonomous Dark Factories."
readme = "README.md"
requires-python = ">=3.9"
license = {file = "LICENSE"}
keywords = ["ai-agents", "langgraph", "mcp", "geopolitics", "risk-analysis", "dark-factories", "graphrag"]
authors = [
    {name = "Vassiliy Lakhonin"}
]
dependencies = [
    "pydantic>=2.0.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "mcp>=0.1.0",
    "langchain-core>=0.1.0",
    "llama-index-core>=0.10.0"
]

[project.scripts]
gtta = "gtta.cli:app"

[project.optional-dependencies]
docs = ["mkdocs-material"]
eval = ["promptfoo"]
ui = ["streamlit>=1.30.0"]
agent = ["langchain-openai", "langchain-anthropic", "langchain-community", "duckduckgo-search", "tavily-python", "langgraph", "langchain-experimental", "networkx"]
test = ["pytest>=7.0.0", "pytest-asyncio"]
dspy = ["dspy-ai"]
enterprise = ["fastapi", "uvicorn", "pypdf", "faiss-cpu", "langchain-huggingface"]

[tool.setuptools.packages.find]
where = ["src"]
